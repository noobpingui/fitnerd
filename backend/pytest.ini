[pytest]
; Restringe donde pytest busca tests a esta carpeta - sin esto, por default
; pytest escanea TODO el proyecto buscando archivos "test_*.py", incluyendo
; .venv (que tiene miles de archivos de librerias), lo cual es lento y
; puede levantar falsos positivos.
testpaths = tests
